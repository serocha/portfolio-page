import React from 'react';

import Title from '../../Waypoints/Title';

import './Masks.scss';

const masks = (props) => (
	<section id='masks'>
		<Title titleWP={props.titleWP} title='masks' navID={3} />
	</section>
);

export default masks;